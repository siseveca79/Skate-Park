<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Datos del perfil - Skate Park</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <style>
        .form-container {
            max-width: 600px;
            margin: auto;
        }
    </style>
</head>
<body>
    <div class="container mt-5">
        <h1 class="text-center">Skate Park</h1>
        <div class="py-4">
            <h2 class="text-center">Datos del perfil</h2>
            <hr class="w-50 mx-auto">
            <div class="alert alert-success text-center" role="alert">
                Bienvenido a su sesión, {{skater.nombre}}
            </div>
            {{#if successMessage}}
                <div class="alert alert-success" role="alert">
                    {{successMessage}}
                </div>
            {{/if}}
            <form action="/profile" method="POST" class="form-container">
                <div class="form-group">
                    <label for="email">Email</label>
                    <input id="email" class="form-control" disabled value="{{skater.email}}">
                </div>
                <div class="form-group">
                    <label for="nombre">Nombre</label>
                    <input id="nombre" class="form-control" name="nombre" value="{{skater.nombre}}">
                </div>
                <div class="form-group">
                    <label for="anos_experiencia">Años de experiencia</label>
                    <input id="anos_experiencia" type="number" class="form-control" name="anos_experiencia" value="{{skater.anos_experiencia}}">
                </div>
                <div class="form-group">
                    <label for="especialidad">Especialidad</label>
                    <input id="especialidad" class="form-control" name="especialidad" value="{{skater.especialidad}}">
                </div>
                <button type="submit" class="btn btn-success btn-block">Guardar</button>
            </form>

            <form action="/logout" method="POST" class="form-container mt-3">
                <button type="submit" class="btn btn-danger btn-block">Logout</button>
            </form>
        </div>
    </div>
</body>
</html>
